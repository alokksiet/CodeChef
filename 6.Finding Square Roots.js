//You are given a positive integer N. Your task is to find the square root of N rounded down to the nearest integer.
function findSquareRoot(n){
    var num = Math.sqrt(n);
    return Math.floor(num);
}
